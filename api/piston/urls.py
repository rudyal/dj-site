from django.conf.urls import *
from api.piston.handlers import *

#picob_handler = Resource(PictureObjectHandler)

urlpatterns = patterns('',
    # ex: /polls/
	#url(r'^$', views.IndexView.as_view(), name='index'),
	#url(r'^viewcontent/$', views.ViewPicOb, name='ViewPicOb'),
	#url(r'^picture$', picob_handler, { 'emitter_format': 'json' }),
	#url(r'^blogpost/(?P<post_slug>[^/]+)/', picob_handler),
	#url(r'^pic/', picob_handler),
)